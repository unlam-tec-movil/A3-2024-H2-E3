package ar.edu.unlam.mobile.scaffolding.ui.screens
/*
import androidx.compose.ui.test.hasClickAction
import androidx.compose.ui.test.junit4.createComposeRule
import androidx.compose.ui.test.onNodeWithText
import androidx.compose.ui.test.performClick*/

class LocationAndMapsScreenTest {
    /*@get:Rule
    val composeTestRule = createComposeRule()

    @Test
    fun testShowLocationButton() {
        // Configurar la pantalla de prueba
        composeTestRule.setContent {
            // GameScreenPreview()
        }
        // Verificar que el botón está presente y tiene la acción de clic
        composeTestRule.onNode(hasClickAction()).assertExists()
        // Hacer clic en el botón
        composeTestRule.onNodeWithText("Mostrar Ubicación").performClick()
        // Verificar que se muestra el mapa
        composeTestRule.onNodeWithText("Se volverá a pedir permiso").assertExists()
    }*/
}
